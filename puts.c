/*
 * scanf"%19s" armazene tudo que o usúario digitar.
 * printf("%s", variavel) - Mostre no prompt de comando esse texto com a variavel inserida naquele local.
 * scanf("%2s", variavel) - Insira na variavel até duas letras.
 */

#include <stdio.h>

int main()
{
    puts("daora codar em c");
    return 0;
}